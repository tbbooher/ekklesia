<div class="row">
  <div class="large-3 medium-4 columns">
    <div class="sidebar">
      <nav>
        <ul class="side-nav">
          <li class="heading">Issues</li>
          <% @issues.each do |issue| %>
            <li><a href="/stances/issues/<%= issue.description %>" data-search><%= issue.description %></a></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
  <%= render partial: "stances/search_results", locals: {stances: @stances} %>
</div>
