<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>openPAC</title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <script src="http://listjs.com/no-cdn/list.js"></script>
    <script src="http://listjs.com/no-cdn/list.fuzzysearch.js"></script>
    <%= csrf_meta_tags %>
    <%= google_webfonts_init({google: ['Montserrat', 'Roboto Slab', 'Roboto']}) %>
  </head>

  <body>
    <nav class="top-bar" data-topbar role="navigation">
    <ul class="title-area">
      <li class="name">
        <h1><%= link_to 'openPAC', root_path, method: :get %></h1>
      </li>
      <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
    </ul>

    <section class="top-bar-section">
      <!-- Left Nav Section -->
      <ul class="left">
      <% if !logged_in? %>
        <li><%= link_to 'Login', login_path, method: :get %></li>
        <li><%= link_to 'Signup', signup_path, method: :get %></li>

      <% else %>
        <li><%= link_to "#{current_user.first_name}'s Profile", user_path(current_user) %></li>
        <li><%= link_to 'Logout', logout_path, method: :get %></li>

      <% end %>
      <li>
      <a data-dropdown="drop1" aria-controls="drop1" aria-expanded="false" data-options="is_hover: true">Stances</a>
        <ul id="drop1" class="f-dropdown" data-dropdown-content aria-hidden="true" tabindex="-1">
          <li><%= link_to 'Stances', stances_path, method: :get %></li>
          <% if !logged_in? %>
            <li><%= link_to 'Take a Stance', login_path, method: :get %></li>
          <% else %>
            <li><%= link_to 'Take a Stance', new_stance_path, method: :get %></li>
          <% end %>
        </ul>
        </li>
        <li><%= link_to 'Legislators', legislators_path, method: :get %></li>
      </ul>

      <!-- Right Nav Section -->
        <ul class="right">
          <!-- Search Bar -->
            <li class="has-form">
              <div class="row collapse ">
                <div class="large-8 small-9 columns">
                  <%= form_tag "/search", method: :get do %>
                  <%= text_field :search, :words, size: 60, class:"radius", placeholder: "search your issue" %>
                </div>
                <div class="large-4 small-3 columns">
                  <%= submit_tag "Search", class: "button radius success" %>
                </div>
                <% end %>
              </div>
            </div>
          </li>
        </ul>
    </section>
  </nav>
  <section role='main' class='scoll-container'>
    <div class='container'>
      <%= yield %>
    </div>
  </section>
    <%= javascript_include_tag "application" %>
  </body>
</html>



